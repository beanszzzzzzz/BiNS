<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders index</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(135deg, #f5e6d3 0%, #d7ccc8 100%); min-height: 100vh; }
        .container { display: flex; min-height: 100vh; }
        /* Sidebar */
        .sidebar { width: 250px; background: linear-gradient(180deg, #6d4c41 0%, #4e342e 100%); padding: 20px; box-shadow: 4px 0 10px rgba(0,0,0,0.1); }
        .sidebar-header { text-align: center; padding: 20px 0; border-bottom: 2px solid #8d6e63; margin-bottom: 30px; }
        .sidebar-header h2 { color: #fff; font-size: 24px; margin-bottom: 5px; }
        .sidebar-header p { color: #bcaaa4; font-size: 12px; }
        .nav-menu { list-style: none; }
        .nav-item { margin-bottom: 10px; }
        .nav-link { display: flex; align-items: center; padding: 15px; color: #efebe9; text-decoration: none; border-radius: 8px; transition: all 0.3s ease; cursor: pointer; }
        .nav-link:hover { background: #5d4037; transform: translateX(5px); }
        .nav-link.active { background: #8d6e63; color: #fff; }
        .nav-icon { margin-right: 15px; font-size: 20px; }
        /* Main Content */
        .main-content { flex: 1; padding: 40px; }
        .header { background: #fff; padding: 25px 30px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); margin-bottom: 30px; }
        .header h1 { color: #4e342e; font-size: 32px; margin-bottom: 5px; }
        .header p { color: #8d6e63; font-size: 14px; }
        .table-container { background: #fff; padding: 30px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .table-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        .table-header h2 { color: #4e342e; font-size: 24px; }
        .btn-new { background: linear-gradient(135deg, #6d4c41 0%, #5d4037 100%); color: #fff; padding: 12px 25px; border: none; border-radius: 8px; cursor: pointer; text-decoration: none; font-size: 14px; transition: all 0.3s ease; }
        .btn-new:hover { transform: translateY(-2px); box-shadow: 0 4px 15px rgba(109, 76, 65, 0.3); }
        table { width: 100%; border-collapse: collapse; }
        thead { background: #efebe9; }
        th { padding: 15px; text-align: left; color: #4e342e; font-weight: 600; border-bottom: 2px solid #d7ccc8; }
        tbody tr { border-bottom: 1px solid #efebe9; transition: background 0.2s ease; }
        tbody tr:hover { background: #fafafa; }
        td { padding: 15px; color: #6d4c41; }
        .empty-state { text-align: center; padding: 60px 20px; color: #8d6e63; }
        .empty-state-icon { font-size: 64px; margin-bottom: 20px; opacity: 0.5; }
        .action-links { display: flex; gap: 10px; }
        .action-links a { color: #6d4c41; text-decoration: none; padding: 5px 12px; border-radius: 5px; transition: all 0.2s ease; font-size: 14px; }
        .action-links a:hover { background: #efebe9; }
    </style>
</head>
<body>
<div class="container">
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2>☕ Cafe Manager</h2>
            <p>Management System</p>
        </div>
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="{{ path('app_dashboard_index') }}" class="nav-link">
                    <span class="nav-icon">📊</span>
                    <span>Dashboard</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_products_index') }}" class="nav-link">
                    <span class="nav-icon">☕</span>
                    <span>Products</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_stocks_index') }}" class="nav-link">
                    <span class="nav-icon">📦</span>
                    <span>Stocks</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_orders_index') }}" class="nav-link active">
                    <span class="nav-icon">🧾</span>
                    <span>Orders</span>
                </a>
            </li>
        </ul>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div class="header">
            <h1>Orders Overview</h1>
            <p>Manage your cafe orders below.</p>
        </div>

        <!-- Table -->
        <div class="table-container">
            <div class="table-header">
                <h2>Orders Records</h2>
                <a href="{{ path('app_orders_new') }}" class="btn-new">+ Add Order</a>
            </div>

            <table>
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Order Number</th>
                        <th>Customer Name</th>
                        <th>Order Date</th>
                        <th>Total Amount</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for order in orders %}
                        <tr>
                            <td>{{ order.id }}</td>
                            <td>{{ order.orderNumber }}</td>
                            <td>{{ order.customerName }}</td>
                            <td>{{ order.orderDate ? order.orderDate|date('Y-m-d H:i:s') : '' }}</td>
                            <td>{{ order.totalAmount }}</td>
                            <td class="action-links">
                                <a href="{{ path('app_orders_show', {'id': order.id}) }}">show</a>
                                <a href="{{ path('app_orders_edit', {'id': order.id}) }}">edit</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6">
                                <div class="empty-state">
                                    <div class="empty-state-icon">🧾</div>
                                    <h3>No orders found</h3>
                                    <p>Add your first order to get started</p>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </main>
</div>
</body>
</html>
