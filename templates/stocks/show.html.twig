<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Details</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5e6d3 0%, #d7ccc8 100%);
            min-height: 100vh;
        }

        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background: linear-gradient(180deg, #6d4c41 0%, #4e342e 100%);
            padding: 20px;
            box-shadow: 4px 0 10px rgba(0,0,0,0.1);
        }

        .sidebar-header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 2px solid #8d6e63;
            margin-bottom: 30px;
        }

        .sidebar-header h2 {
            color: #fff;
            font-size: 24px;
            margin-bottom: 5px;
        }

        .sidebar-header p {
            color: #bcaaa4;
            font-size: 12px;
        }

        .nav-menu {
            list-style: none;
        }

        .nav-item {
            margin-bottom: 10px;
        }

        .nav-link {
            display: flex;
            align-items: center;
            padding: 15px;
            color: #efebe9;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .nav-link:hover {
            background: #5d4037;
            transform: translateX(5px);
        }

        .nav-link.active {
            background: #8d6e63;
            color: #fff;
        }

        .nav-icon {
            margin-right: 15px;
            font-size: 20px;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            padding: 40px;
        }

        .header {
            background: #fff;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        .header h1 {
            color: #4e342e;
            font-size: 32px;
            margin-bottom: 5px;
        }

        .header p {
            color: #8d6e63;
            font-size: 14px;
        }

        /* Details Container */
        .details-container {
            background: #fff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            max-width: 800px;
        }

        .details-table {
            width: 100%;
        }

        .details-table tr {
            border-bottom: 1px solid #efebe9;
        }

        .details-table tr:last-child {
            border-bottom: none;
        }

        .details-table th {
            text-align: left;
            padding: 20px 20px 20px 0;
            color: #4e342e;
            font-weight: 600;
            width: 200px;
            font-size: 16px;
        }

        .details-table td {
            padding: 20px;
            color: #6d4c41;
            font-size: 16px;
        }

        /* Buttons */
        .action-buttons {
            display: flex;
            gap: 15px;
            margin-top: 30px;
            padding-top: 30px;
            border-top: 2px solid #efebe9;
            flex-wrap: wrap;
        }

        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #6d4c41 0%, #5d4037 100%);
            color: #fff;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(109, 76, 65, 0.3);
        }

        .btn-secondary {
            background: #efebe9;
            color: #4e342e;
        }

        .btn-secondary:hover {
            background: #d7ccc8;
        }

        .btn-danger {
            background: #d32f2f;
            color: #fff;
        }

        .btn-danger:hover {
            background: #b71c1c;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(211, 47, 47, 0.3);
        }

        /* Delete form inline */
        .delete-form {
            display: inline;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2>BiNS Cafe Manager</h2>
                <p>Management System</p>
            </div>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="{{ path('app_dashboard_index') }}" class="nav-link">
                        <span class="nav-icon"></span>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_products_index') }}" class="nav-link">
                        <span class="nav-icon"></span>
                        <span>Products</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_stocks_index') }}" class="nav-link active">
                        <span class="nav-icon"></span>
                        <span>Stocks</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <span class="nav-icon"></span>
                        <span>Orders</span>
                    </a>
                </li>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="header">
                <h1>Stock Details</h1>
                <p>View stock information</p>
            </div>

            <div class="details-container">
                <table class="details-table">
                    <tbody>
                        <tr>
                            <th> ID</th>
                            <td>{{ stock.id }}</td>
                        </tr>
                        <tr>
                            <th> Quantity</th>
                            <td>{{ stock.quantity }}</td>
                        </tr>
                        <tr>
                            <th> Date Added</th>
                            <td>{{ stock.dateAdded ? stock.dateAdded|date('Y-m-d H:i:s') : 'N/A' }}</td>
                        </tr>
                    </tbody>
                </table>

                <div class="action-buttons">
                    <a href="{{ path('app_stocks_index') }}" class="btn btn-secondary">⬅ Back to list</a>
                    <a href="{{ path('app_stocks_edit', {'id': stock.id}) }}" class="btn btn-primary">✏️ Edit</a>
                    
                    <form method="post" action="{{ path('app_stocks_delete', {'id': stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-form">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id) }}">
                        <button type="submit" class="btn btn-danger">🗑️ Delete</button>
                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>